interface Window {
  // add index signature to Window
  [k: string]: any;

  SJ_TAB_FACET_SEARCH_DISABLED: boolean;
  sajari: {
    ui: any[];
  };
}

interface Document {
  // add index signature to Document
  [k: string]: any;
}
